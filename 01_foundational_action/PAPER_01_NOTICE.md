# NOTICE — `PAPER_01` is quarantined

**File:** `01_foundational_action/PAPER_01_MU_DERIVATION_ACTION.tex`
**Ledger:** `EPISTEMIC_BOUNDARY_v1.5.0.md` item **D1.1**
**Status:** not current theory. Fatal under the withdrawn zero-parameter standard and under the two-parameter standard.

The single-channel branch whose interpolating function is effectively `arcsinh` (or whose Euler–Lagrange map inverts the Newtonian / deep-MOND limits) is correspondence-false:

`\mu \to 0` as `x \to \infty`, `\mu \to 1` as `x \to 0`.

That is the opposite of a viable MOND interpolating function. Target D1 records the no-go. Dual-channel `F_{\mathrm{dual}}(x)=\tfrac12 x^2-x+\ln(1+x)` is the live `[P]` identity.

Do not cite `PAPER_01` in an abstract, README headline, or referee letter as the derivation of `μ`. Leave the TeX in place as a dated artifact so the error is auditable.
